machine:
  environment:
    CXX: g++-5
dependencies:
  pre:
  # - yes y | sudo add-apt-repository ppa:ubuntu-toolchain-r/test
  - sudo apt-get update; sudo apt-get -y -qq install qt5-default
checkout:
  post:
  - git submodule init
  - git submodule update --depth 1 3rdparty/ubuntu
test:
  override:
  - echo "Hello Circle!"
