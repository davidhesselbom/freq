machine:
  environment:
    CXX: g++-5
dependencies:
  pre:
  - yes y | sudo add-apt-repository ppa:ubuntu-toolchain-r/test
  - sudo apt-get -y -qq install libgles2-mesa-dev libegl1-mesa-drivers libgl1-mesa-dri mesa-common-dev libglu1-mesa-dev build-essential qt5-default
checkout:
  post:
  - git submodule init
  - git submodule update --depth 1 3rdparty/ubuntu
test:
  override:
  - echo "Hello Circle!"
